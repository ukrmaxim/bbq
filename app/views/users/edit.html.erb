<h1><%= t('Edit profile') %></h1>

<div class="row">
  <div class="col-md-10 buffer-top">
    <%= render 'form' %>

    <p class="buffer-top">
      <%# Ссылка на смену пароля текущего залогиненного юзера (этот путь генерит девайз) %>
      <%= link_to t('Change password'), edit_user_registration_path, class: 'btn btn-secondary btn-block' %>
    </p>
    </div>
</div>
